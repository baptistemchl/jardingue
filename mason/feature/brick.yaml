name: feature
description: Génère une nouvelle feature pour Jardingue avec l'architecture standard

version: 0.1.0

environment:
  mason: ">=0.1.0-dev.58 <0.1.0"

vars:
  name:
    type: string
    description: Nom de la feature (en snake_case, ex: plant_detail)
    prompt: "Nom de la feature (snake_case) :"
